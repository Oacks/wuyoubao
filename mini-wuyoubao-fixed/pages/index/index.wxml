<!-- index.wxml -->
<view class="container">
  <navBar title="" back="{{false}}" home="{{false}}" background="{{'#3EC755'}}">
    <view class="white ml20 barTitle" slot="center">无忧保客户端</view>
  </navBar>
  <view class="top-container index-bg">
          <!-- <view class="login-title">
          无忧保
        </view> -->
    <view class="svg-left"></view>
    <view class="svg-right"></view>
  </view>

  <!-- <view class="swiper-container"> -->
    <!-- <swiper class="swiper" circular="{{true}}" autoplay="{{true}}" indicator-dots="{{indicatorDots}}" bindchange="swiperChange" previous-margin="{{previousmargin}}" next-margin="{{nextmargin}}">
      <block wx:for="{{imgsArr}}" wx:key="index">
        <swiper-item>
          <image class="{{index == currentSwiper ? ' active' : ' quiet'}}" src="{{item.url}}"></image>
        </swiper-item>
      </block>
    </swiper> -->
    <!-- 重置小圆点的样式 -->
    <!-- <view class="dots">
      <block wx:for="{{imgsArr}}" wx:key="index">
        <view class="dot{{index == currentSwiper ? ' active' : ''}}"></view>
      </block>
    </view> -->
  <!-- </view> -->

  <!-- 按钮 -->
  <view class="btn-container">
    <view class="btns-box">
      <view class="center-box" bind:tap="order">
        <view class="pic-box">
          <image class="btns-pic center-box special-pic" src="/images/note.png">
          </image>
        </view>
        <text class="btns-word word-color">
          报障
        </text>
      </view>
    </view>
  </view>

</view>